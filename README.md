# MathIXL

A Vite + React + TypeScript app that delivers a simple 15-question practice quiz. Configure grade, point, and number limits on the home page, then start a session with a unique code that persists in `localStorage` so progress survives refreshes.

## Available Scripts

- `npm install`
- `npm run dev`
- `npm run build`
